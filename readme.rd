# Movies & Weather GraphQL Backend

See `README.md` for the full minimal setup, env, and usage.

Quick start:
- Install: `yarn`
- Configure: copy `.env.example` to `.env` and fill values
- Run: `yarn dev` → GraphQL at `http://localhost:<PORT>/graphql`

